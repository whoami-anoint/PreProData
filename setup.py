from setuptools import setup, find_packages

setup(
    name='PreProData',
    version='0.2',
    packages=find_packages(),
    install_requires=[
        'scikit-learn',  # You can add more dependencies here
    ],
)
